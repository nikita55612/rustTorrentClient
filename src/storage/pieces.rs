// use crate::{storage::piece::Piece, util::Hash};

// pub struct Pieces {
//     buf: Vec<Piece>,
//     hashes: Vec<Hash>,
//     piece_len: usize,
// }

// impl Pieces {
//     pub fn xxx(&self, index: usize, hash: &Hash) -> bool {
//         Some(hash) == self.hashes.get(index)
//     }
// }
